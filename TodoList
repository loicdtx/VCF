General:
- Warping utilities can accommodate 2 functions for different ways of doing
- Implement a gap filling module that fills remaining clouds with the MODIS vcf
- funcion warp() is in progress ...

GetPR:
- Allow querying using extent object

downloadPR:
- better document the fact that a list of path/rows can be used instead of the standard object (See mail from Valerio)

warpVCF:
- Find a way to deal with overlapping areas (first warp without stitching together, then mosaic using mosaic{raster})
- Intermediary should as much as possible be written to integer (should be flexible based on the data range)

warp:
- Document the function
- Test and validate the function

